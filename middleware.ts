
// File này đã được vô hiệu hóa vì Static Export không hỗ trợ Middleware.
// Cloudflare Pages sẽ xử lý định tuyến trực tiếp qua các tệp HTML tĩnh.
export default function middleware() {}
export const config = { matcher: [] };
