# ==============================
# Netlify build config for Next.js full site + Decap CMS
# ==============================

[build]
  command = "npm run build"
  publish = ".next"  # 👈 hoặc "out" nếu bạn dùng "next export"

# Nếu bạn dùng Next.js 13+ App Router (build mặc định SSR)
# thì giữ ".next" — Netlify sẽ tự xử lý qua plugin Next.js.

# --------------------------------
# Cho phép xác thực từ domain chính
# --------------------------------
[[redirects]]
  from = "https://wheeledit.com/*"
  to = "https://wheeledit.netlify.app/:splat"
  status = 200
  force = true

[[redirects]]
  from = "https://cms.wheeledit.com/*"
  to = "https://wheeledit.netlify.app/:splat"
  status = 200
  force = true

[[redirects]]
  from = "https://wheeledit.pages.dev/*"
  to = "https://wheeledit.netlify.app/:splat"
  status = 200
  force = true

# --------------------------------
# Security headers
# --------------------------------
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "SAMEORIGIN"
